<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zapateria">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivityFactura" />
        <activity
            android:name=".MainActivityNuevaSolicitud"
            android:label="@string/title_activity_main_nueva_solicitud" />
        <activity
            android:name=".MainActivityNuevoCliente"
            android:label="@string/title_activity_main_nuevo_cliente" />
        <activity
            android:name=".MainActivitySolicitudes"
            android:label="@string/title_activity_main_solicitudes" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>